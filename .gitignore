# 虚拟环境
.venv/
__pycache__/
*.pyc

# 临时文件
.hermes-tmp.*
data/.hermes-tmp.*

# 业务数据库(每台机器各自本地数据,不同步)
data/*.db
data/*.db-shm
data/*.db-wal
data/*.db.bak_*
data/*.db.init-shm
data/*.db.init-wal
backup/

# 公网隧道地址(每台机器不同)
data/public_url.txt

# Windows 遗留/运行产物
data/cloudflared.exe
data/_*.png
data/_*.vbs
data/callback_registered.txt
data/watchdog.py

# 备份文件
*.bak_*
.DS_Store

# V11.126: 运行日志
app_run*.log
app_watchdog2.log

# Mac误提交的测试凭证附件(冒号文件名Windows无法检出)
uploads/voucher_*.xlsx

# 运行日志/隧道地址(不入库, 防自动部署误判本地改动)
data/app_watchdog.log
data/serveo_url.txt
