# 飞书工作台 + 统一认证 + 虾工具 · 实施方案

## 一、总体架构

```
山西正成能源 · 飞书组织（全员）
        │
        ▼ 工作台点击「采购系统」
┌─────────────────────────────────────────┐
│ 飞书网页应用 (OAuth 统一认证)            │
│  员工点开 → 飞书自动授权 → 免密登录系统   │
└──────────────────┬──────────────────────┘
                   │
        ┌──────────┴──────────┐
        ▼                     ▼
┌────────────────┐   ┌────────────────┐
│ 系统本体(app.py)│   │ 虾(xia.py)     │
│ 只做业务流转:   │   │ 独立提醒服务:   │
│ 申请/订单/合同/ │   │ 业务预警/待办/  │
│ 送货/入库/挂账/ │   │ 日报/周报       │
│ 付款/库存/报表  │   │ 推领导+负责人   │
└────────────────┘   └────────────────┘
        │                    ▲
        └──── 读同一数据库 ────┘
```

分工原则：**系统本体做基础业务流转，虾专职提醒**（预警/报表/待办/汇总全交给虾），互不干扰。

## 二、部署A：飞书工作台接入 + 统一认证

### 2.1 创建/配置网页应用（开放平台，约20分钟）
1. 开放平台 → 企业自建应用（已有"正成采购系统"则直接编辑）
2. 应用能力 → 网页应用：URL 填系统地址
3. **安全设置 → 重定向 URL**：填 `系统地址/api/feishu/oauth/callback`（统一认证回调，必须与系统内一致）
4. 权限管理开通：`contact:user.base:readonly`、`contact:user.id:readonly`（获取用户身份）
5. 发布 → 管理员审核 → 工作台设置里设为工作台应用（全员可见）

### 2.2 员工使用流程（统一认证已内置，代码就绪）
```
员工打开飞书 → 工作台 → 点「正成能源采购系统」图标
→ 首次点"同意授权"（一次） → 自动登录进入系统（免输账号）
→ 按角色看到对应功能（部门负责人有审批、财务有挂账付款…）
```
- 登录匹配规则：飞书 open_id → 系统用户（管理员提前在「飞书设置→用户绑定」绑定）
- 未绑定员工首次点开：提示联系管理员绑定，绑定后即可用
- 账号密码登录仍保留（备用通道）

### 2.3 全员启用前提（管理员做一次）
| 事项 | 位置 |
|------|------|
| 每个员工飞书 open_id 绑定 | 系统「飞书设置 → ② 用户飞书绑定」按手机号一键查询绑定 |
| 审批角色（部门负责人/财务/领导）必须绑定 | 同上（审批推送依赖绑定） |
| 领导 open_id 配置 | 系统配置 xia_leader_open_ids（逗号分隔），不填则自动取"总经理/分管领导"角色 |

## 三、配套协同工具「虾（龙虾）」

### 3.1 定位
独立于系统本体的提醒服务，由计划任务每 5 分钟唤醒，读系统数据库 + 飞书机器人推送。
**系统本体零提醒逻辑，全交虾。**

### 3.2 推送规则（全部可调）

| 类型 | 触发 | 推送给 | 说明 |
|------|------|--------|------|
| 业务预警·审批超时 | 待审批超 24h | 对应审批角色 | 超 48h 升级给管理员 |
| 业务预警·待采未下单 | 申请通过 3 天未转订单 | 申请人 | |
| 业务预警·逾期未回货 | 送货单超期未签收 / 订单超目标日 | 订单负责人 | |
| 待办提醒 | 每天 09:00 | 领导 | 今日待审批/待签收/待付款/库存预警汇总 |
| 周期日报 | 每天 18:00 | 领导 | 当日新增订单金额/入库/付款 |
| 每周汇总 | 周一 09:00 | 领导+报表群 | 周采购总额/待审批/预警/超时未签收 |
| 库存预警 | 低于安全库存 | 对应品类采购（现有） | |

### 3.3 虾的部署
- 脚本：`xia.py`（已就绪），独立运行，不依赖系统进程
- 计划任务：ZCE_Xia 每 5 分钟运行一次（已注册）
- 开关：系统配置 `xia_enabled=1`（默认开）；系统本体预警调度自动让位，杜绝双推
- 去重：同一提醒每日只推一次（reminder_log 表）

## 四、你要在飞书后台做的（一次性，约30分钟）
1. 确认企业组织为「山西正成能源」飞书版，全员已加入
2. 开放平台应用：网页应用 URL + 重定向 URL + 权限 + 发布审核（2.1 节）
3. 管理后台：应用设为工作台应用、全员可见
4. 系统内：填 App ID/Secret（已有）→ 绑定全员 open_id → 确认虾已启用

完成后：全员在飞书里点图标即用，虾自动盯单推领导，无需任何手工提醒。
