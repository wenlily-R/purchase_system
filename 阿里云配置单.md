# 阿里云部署配置单（68元/月档）

## 一、服务器选型

| 项目 | 推荐配置 | 说明 |
|------|---------|------|
| 产品 | 阿里云·轻量应用服务器 | 无需运维经验，控制台一键部署 |
| 规格 | 2核 CPU / 2G 内存 | 采购系统+数据库绰绰有余 |
| 磁盘 | 40GB SSD（ESSD） | 数据库+附件够用 |
| 带宽 | 3Mbps 峰值 | 内网办公场景够用 |
| 系统 | Ubuntu 22.04 LTS | 部署脚本已验证 |
| 费用 | 约 68 元/月（新用户有 1-3 折优惠，实际可能更低） | 可按月/按年付费 |
| 可选 | 绑定域名（约 60 元/年） | 有域名体验更正规，没有也可以用 IP |

## 二、购买步骤（10分钟）

1. 登录阿里云 → 搜索"轻量应用服务器" → 立即购买
2. 地域选离公司近的（山西 → 华北2 北京 或 华北1 青岛）
3. 套餐选 2核2G（68元档）；镜像选 **Ubuntu 22.04**
4. 设置 root 密码（记好，部署要用）
5. 购买完成后：控制台 → 该服务器 → 复制"公网 IP"
6. 防火墙/安全组：放行 **80 端口**（HTTP）和 **22 端口**（SSH，默认已开）

## 三、部署（20分钟，我可以远程代做）

1. 把 `采购系统服务器部署包.zip` 传上去：`scp 采购系统服务器部署包.zip root@服务器IP:/root/`
2. 解压 → 装依赖（部署包内已附完整脚本说明）
3. nginx 反代 80 端口 → 127.0.0.1:5899
4. 访问 `http://服务器IP` 即用（账号 admin/admin123）
5. 飞书工作台/回调地址统一指向 `http://服务器IP`

## 四、两套并行试运行安排

| | 部署A 飞书工作台 | 部署B 阿里云 |
|---|---|---|
| 入口 | 飞书 App → 工作台 → 采购系统 | 浏览器 http://服务器IP |
| 认证 | 飞书企业组织统一认证 | 账号密码 |
| 用途 | 全员日常使用 | 数据备份+备用入口 |
| 数据 | 同一数据库，阿里云为主存储 | 本地留存备份 |

> 说明：两套系统共用同一份数据（阿里云为主），飞书工作台通过网页应用挂载指向阿里云地址，本质是同一系统两个入口，不产生数据分裂。试运行 1 个月后择优长期使用。
