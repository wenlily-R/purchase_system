正成能源采购系统 V9.0 交接安装包
================================

一、包内容
  - app.py                系统主程序(Flask)
  - templates/            前端页面(单页应用)
  - data/purchase.db.init 初始数据(含演示业务数据: 申请/订单/合同/库存/流水)
  - data/                 配置、守护脚本、隧道程序
  - uploads/              附件上传目录
  - requirements.txt      Python 依赖清单
  - 一键安装.bat          一键安装+导入数据+启动 (推荐)
  - 安装部署说明.md        本文档

二、环境要求
  - Windows 10/11
  - Python 3.11+ (未安装请到 python.org 下载, 安装时勾选 Add to PATH)

三、一键安装 (推荐)
  1. 解压本包到目标目录(路径建议无中文无空格, 如 D:/purchase_system)
  2. 双击「一键安装.bat」
  3. 脚本自动完成: 检查Python → 创建虚拟环境 → 安装依赖 → 导入数据 → 启动系统
  4. 浏览器访问 http://127.0.0.1:5899
     默认账号: admin / admin123 (首次登录后请修改密码)

四、手动安装 (可选)
  1. python -m venv .venv
  2. .venv/Scripts/pip install -r requirements.txt
     (如无pip: 用 uv pip install --python .venv/Scripts/python.exe -r requirements.txt)
  3. 导入数据: copy data\purchase.db.init data\purchase.db
  4. 启动: .venv/Scripts/python.exe app.py

五、公网访问(可选)
  - 方式A(免费隧道, 地址会变): data/cloudflared.exe tunnel --url http://127.0.0.1:5899
    启动后从输出找 https://xxx.trycloudflare.com 地址, 写入 data/public_url.txt
  - 方式B(固定地址): 部署到公司服务器, 用 Nginx 反代或云厂商隧道

六、钉钉接入配置
  - 登录系统 → 系统设置 → 钉钉, 填入 AppKey/AppSecret
  - 模板"采购申请审批"已配置发起人自选(TARGET_SELECT), 审批人由系统审批流决定
  - 审批人绑定: 系统设置 → 钉钉 → 手机号查询 → 绑定 dingtalk_userid
  - 审批流配置: 系统设置 → 审批流(每类单据多级审批, 审批人填用户名)

七、守护与自动恢复(可选)
  - data/watchdog.py 每5分钟检查系统+隧道, 异常自动拉起
  - 计划任务: schtasks /create /tn ZCE_Purchase_Watchdog /tr "pythonw watchdog.py" /sc minute /mo 5

八、数据备份
  - 数据库: data/purchase.db (SQLite, 直接复制即备份)
  - 系统每日 03:00 自动备份到 data/ 目录

九、注意事项
  - 修改 app.py 或模板后需重启进程生效
  - 杀进程只杀 app.py 与 cloudflared, 勿动其他 Python 进程
  - 钉钉附件上传需在钉钉开发者后台开通 Storage.Space.Write / Storage.UploadInfo.Read 权限
