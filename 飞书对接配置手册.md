# 正成能源智慧采购系统 · 飞书对接配置手册（V4.1）

> 本文档说明如何把采购系统（`C:\Users\mjj\Desktop\purchase_system`）的审批与预警对接飞书，
> 实现"类似飞书请假审批"的移动端线上审批 + 飞书机器人自动预警。

---

## 一、整体架构

```
┌──────────────┐   创建单据(申请/订单/合同/挂账/付款)    ┌──────────────────┐
│  采购系统     │ ──────────────────────────────────────▶ │  飞书审批应用      │
│  (Flask+SQLite)│  自动发起审批实例(node_approver指定审批人)│  (5个审批定义)     │
│  http://127.0.0.1:5899                                  │                    │
└──────┬───────┘                                          └────────┬─────────┘
       │                                                          │ 审批人在手机飞书
       │  ① 事件回调: 审批通过/驳回                                │ 处理审批
       │  /api/feishu/callback ◀────────── 飞书事件订阅 ───────────┤
       ▼                                                          ▼
┌──────────────────┐   ② 预警调度线程(每60秒)               ┌──────────────────┐
│ 系统数据库状态     │ ──▶ 超时未审批/待采未下单/逾期未回货/周报 ──▶ │ 飞书机器人         │
└──────────────────┘         机器人卡片推送                 │ (推个人+推群)     │
                                                           └──────────────────┘
```

要点：
- **审批在飞书里办**：系统创建单据后自动在飞书发起审批实例，审批人在飞书 App 的"审批"应用中处理（类似请假审批），结果通过事件回调自动同步回系统，系统内审批中心仍可用（双通道，以先操作者为准）。
- **预警全部走飞书机器人**：系统内不开发提醒功能。超时未审批、待采未下单、逾期未回货、每周周报，全部由机器人推送到对应负责人。

---

## 二、前置条件

| # | 条件 | 说明 |
|---|------|------|
| 1 | 飞书企业版 | 需要有企业管理后台权限（开通应用、授权审批能力） |
| 2 | 审批应用已开通 | 飞书管理后台 → 工作台 → 审批（飞书默认已开通，确认即可） |
| 3 | 回调地址可达 | 事件回调需要公网可访问的地址（3 种方式见第四节） |

---

## 三、第一步：创建自建应用（约10分钟）

1. 登录 [飞书开放平台](https://open.feishu.cn/) → 开发者后台 → 创建企业自建应用
2. 应用名称：`正成采购系统`；描述随意
3. 创建后进入应用详情：
   - **凭证与基础信息** 页：记下 **App ID** 和 **App Secret**（填到系统"飞书设置"里）
   - **安全设置** 页：
     - 重定向 URL 不用配（非网页应用）
     - **Verification Token**：点击"生成"，记下（或自己填一串随机字符串）
     - **Encrypt Key**：点击"生成"，记下。**建议加密策略选"不加密"**（见 FAQ），这样 Encrypt Key 可留空
4. **添加应用能力** → 机器人：启用"机器人"能力（预警推送用）

## 四、第二步：权限配置

应用详情 → **权限管理** → 开通以下权限：

| 权限码 | 用途 |
|--------|------|
| `approval:approval` | 创建审批定义、发起审批实例（核心） |
| `approval:instance` | 审批实例相关（保险起见一起开） |
| `im:message:send_as_bot` | 机器人发消息（预警/通知） |
| `contact:user.base:readonly` | 按手机号查询用户（绑定 open_id 用） |
| `contact:user.id:readonly` | 获取用户 open_id |

> 权限开通后需要**企业管理员在"管理后台 → 应用管理"里审核通过**（自建应用一般免审，若企业设了审批流则需管理员点同意）。所有权限开完，**在权限管理页点"更新版本并发布"**，再在管理后台通过，否则接口会报权限错误。

## 五、第三步：事件订阅（审批结果回传的关键）

应用详情 → **事件与回调**：

1. 请求地址：填回调 URL（三选一，见下）
2. 订阅事件，搜索并勾选：
   - **审批实例状态变更**（事件名 `approval_instance`）— 必选
   - 审批任务状态变更（`approval_task`）— 可选
3. 加密策略：选"**不加密**"（最简单）；若选加密，需安装 `pip install pycryptodome` 并把 Encrypt Key 填入系统
4. 保存后点"验证"：飞书会 GET 你的回调地址，系统自动应答 challenge，显示"验证通过"即成功

### 回调地址三选一（系统默认监听 127.0.0.1:5899）

**方式A：内网穿透（推荐，免费，本机即可）**
1. 注册 [natapp.cn](https://natapp.cn) 或 花生壳、ngrok
2. 买/申请一个免费隧道，本地端口填 **5899**
3. 得到公网地址如 `https://xxxx.natapp.cc`
4. 回调地址填：`https://xxxx.natapp.cc/api/feishu/callback`
5. 注意：免费隧道域名会变，变了要回来更新；想长期稳定买固定域名

**方式B：部署到公司公网服务器**
1. 把 purchase_system 整个文件夹拷到服务器
2. 服务器装 Python + `pip install flask flask-cors`
3. 启动：`python app.py`（默认 5899 端口）
4. 回调地址填：`http://服务器IP:5899/api/feishu/callback`
5. 生产建议：用 nginx 反代加 https

**方式C：内网长连接（后续可选项）**
飞书支持 WebSocket 长连接接收事件，无需公网。需要安装官方 SDK `pip install lark-oapi` 并额外写一个桥接脚本，把事件转发给本机 `/api/feishu/callback`。当前版本未内置，如需可再开发。

## 六、第四步：创建审批定义（二选一）

### 方式A：系统一键创建（推荐先试）
系统"飞书设置"页 → ① 填好 App ID/Secret → ② 绑定至少一个用户的 open_id（管理员）→ 点 **"一键创建审批定义"**。系统会自动创建 5 个审批流：

| 审批流 | 审批代码 | 表单控件ID |
|--------|---------|-----------|
| 采购申请审批 | `zhengcheng_purchase_request` | SQ_no / SQ_name / SQ_amount / SQ_applicant / SQ_date |
| 采购订单审批 | `zhengcheng_purchase_order` | CG_no / CG_name / CG_amount / CG_applicant / CG_date |
| 合同审批 | `zhengcheng_contract` | HT_no / HT_name / HT_amount / HT_applicant / HT_date |
| 挂账审批 | `zhengcheng_credit` | GZ_no / GZ_name / GZ_amount / GZ_applicant / GZ_date |
| 付款审批 | `zhengcheng_payment` | FK_no / FK_name / FK_amount / FK_applicant / FK_date |

### 方式B：手工在飞书审批后台创建（若一键创建报错）
1. 打开飞书 → 审批 → 管理后台 → 创建审批流程
2. 按上面表格建 5 个流程，每个流程的表单加 5 个控件，**控件 ID（字段标识）必须与表格一致**（在审批后台表单设计里每个控件可设置"标识"）
3. 审批人节点：建 **3 个顺序审批节点**（系统会按实际审批链路逐节点指定审批人，链路不足 3 级的余下节点自动归最后一位审批人）
4. 保存发布后，进入审批流设置 → 基本信息 → 复制"审批代码 approval_code"
5. 把 5 个审批代码按 biz_type 填入系统数据库：
   ```
   sqlite3 data/purchase.db
   INSERT OR REPLACE INTO sys_config(key,value) VALUES('feishu_approval_codes',
     '{"purchase_request":"<代码1>","purchase_order":"<代码2>","contract":"<代码3>","credit":"<代码4>","payment":"<代码5>"}');
   ```

## 七、第五步：系统内配置（5 分钟）

1. 启动系统（双击 `启动系统.bat`），用 admin 登录
2. 左侧菜单（仅管理员可见）→ **✈️ 飞书**
3. ① 应用配置：
   - 填 App ID / App Secret / Verification Token
   - Encrypt Key：选"不加密"就留空
   - 报表推送群 chat_id：可选（群设置里获取，供周报推送；不填则只推给管理员）
   - 审批超时阈值默认 24 小时、申请转订单默认 3 天，可改
   - 勾选"启用飞书对接" → 保存配置
4. 点 **🔌 测试连接**：显示"连接成功 token=xxx"即 API 凭证 OK
5. ② 用户飞书绑定：给每个用户填手机号 → 点"查询"自动带出 open_id → 点"保存"
   （必须给审批角色用户绑定：部门负责人/财务/分管领导/总经理/系统管理员，以及申请人和订单负责人）
6. ③ 对接状态里确认：启用、配置完整、已绑定人数、审批定义数量

## 八、第六步：端到端验证

1. 用普通员工账号（如 刘员工/123456）提交一笔采购申请
2. 系统自动在飞书发起审批实例，**第一审批人收到飞书消息卡片提醒**
3. 审批人打开飞书 → 审批 → 看到该申请（含单据编号/金额/申请人）→ 同意/驳回
4. 回系统看：申请状态自动变为"已通过"/"已驳回"（回调同步，通常秒级）
5. 管理员在"飞书设置 → ④ 立即执行一次预警"可手动触发预警扫描，验证机器人推送

审批金额链路（与系统内审批流一致）：

| 金额区间 | 审批链 |
|---------|--------|
| ≤ 5,000 元 | 部门负责人 |
| 5,000 ~ 20,000 元 | 部门负责人 → 财务 |
| 20,000 ~ 50,000 元 | 部门负责人 → 财务 → 分管领导 |
| ≥ 50,000 元 | 部门负责人 → 财务 → 分管领导 → 总经理 |

---

## 九、预警提醒规则（全部由飞书机器人推送，系统内不开发提醒）

| 规则 | 触发条件 | 推送对象 | 卡片颜色 |
|------|---------|---------|---------|
| 超时未审批 | 审批待处理超过阈值（默认24小时） | 对应审批角色（按角色绑定人） | 橙 |
| 审批升级 | 审批超时 ≥ 48 小时 | 系统管理员（可再加报表群） | 红 |
| 待采未下单 | 采购申请已通过超过 N 天（默认3天）仍未转采购订单 | 申请人 | 橙 |
| 逾期未回货 | 送货单计划到货日已过仍未签收 | 订单负责人 | 红 |
| 订单超期 | 订单目标日已过仍未完成 | 订单负责人 | 橙 |
| 每周周报 | 每周一 09:00 | 报表群 + 系统管理员 | 蓝 |

调度：系统后台每 60 秒扫描一次；同一规则同一单据**每天只推一次**（防轰炸）。
阈值可在"飞书设置"里调整；`reminder_log` 表记录每日已推送项。

---

## 十、常见问题 FAQ

**Q1: 测试连接报"获取tenant_access_token失败"**
- App ID/Secret 抄错了；或应用未发布/未通过管理员审核（第四节第3步）

**Q2: 一键创建审批定义报权限错误**
- 确认已开 `approval:approval` 且已发布；企业需开通飞书"审批"应用
- 仍不行 → 改用"方式B手工创建"，把审批代码填入 sys_config

**Q3: 发起审批实例报错（节点数量不匹配）**
- 系统已做自动补齐重试；若仍报错，把手工审批定义调整成 3 个顺序审批节点
- 表单控件 ID 必须与第六节表格一致（SQ_no 等），否则报控件不存在

**Q4: 回调地址验证不通过**
- 确认回调地址公网可达：在浏览器/手机网络打开 `http(s)://你的地址/api/feishu/callback` 能看到 JSON
- 内网穿透隧道没映射到 5899 端口；或防火墙挡了
- Verification Token 与系统里填的不一致（GET 验证时系统会校验 token）

**Q5: 审批通过后系统状态没变**
- 检查"飞书设置 → ③ 对接状态"的"待回调同步"数量：>0 说明回调没收到
- 确认事件订阅里勾了"审批实例状态变更"且验证通过
- 看系统操作日志（日志页）有无"飞书审批同步"记录
- 排查本机防火墙/隧道是否把 POST 事件也转发了（只验证 GET 不行）

**Q6: 事件回调带 encrypt 字段报解密失败**
- 飞书后台加密策略选了"加密"但没装解密库：`pip install pycryptodome` 后重启系统
- 或把加密策略改为"不加密"，Encrypt Key 留空

**Q7: 审批人收不到消息提醒**
- 该用户的 open_id 未绑定；机器人权限 `im:message:send_as_bot` 未开
- 审批实例虽已发起，但飞书审批自带"审批待办"通知，实际审批在飞书【审批】应用里一定能看到

**Q8: 系统里审批和飞书审批都点，会重复吗？**
- 不会：审批结果以"先完成者"为准，`approval_instances` 和 `feishu_instances` 都有幂等保护，后到者自动忽略

**Q9: 已有历史待审批单据要补发到飞书？**
- 管理员 → 飞书设置 → ④ "补发未同步审批"，一键把系统内所有待审批但未发飞书的单据补发

**Q10: 担心本机测试污染数据？**
- 用 `curl -X POST http://127.0.0.1:5899/api/reset-db` 可一键重置数据库（生产慎用）

---

## 十一点五、当前公网访问地址

> 系统已通过 Cloudflare 免费隧道对外发布（无需注册）：
>
> **https://earthquake-legacy-webshots-determined.trycloudflare.com**
>
> 任何人打开此链接即可访问系统（账号 admin / admin123）。
>
> ✅ 系统已配置**开机自启+自动守护**（计划任务 ZCE_Purchase_OnLogon / ZCE_Purchase_Watchdog）：
> 电脑重启后自动恢复系统与隧道，每5分钟自检，进程挂了自动拉起，新地址自动写入 `data\public_url.txt`，
> 系统内「飞书设置 → ③ 对接状态」页可随时查看当前公网地址。
>
> ⚠️ 免费隧道限制：**每次隧道进程重启地址都会变**（已发出去的旧链接会失效）。
> 要永久固定地址：① natapp.cn 付费固定域名（约9元/月，国内速度快）② 公司公网服务器部署。配置好固定域名后告诉我，我把系统指过去。

## 十一、上线注意事项

1. **关闭 debug**：正式使用时把 `app.run(host='127.0.0.1', port=port, debug=True)` 改为 `debug=False`，并换掉 `app.secret_key`
2. **回调地址维护**：内网穿透免费隧道域名变更后，必须回飞书后台更新请求地址
3. **审批定义维护**：审批流里加控件/改节点后，审批代码不变则系统无需改动
4. **数据库备份**：定期备份 `data/purchase.db`（可加 Windows 计划任务）
5. **权限最小化**：给审批角色的用户在系统里按角色开权限；飞书侧应用权限由管理员审核

---

*版本：V4.1 飞书对接版 · 2026-08 · 正成能源智慧采购系统*


## 安全说明(2026-08-04)
- 系统已锁定：除 admin 外所有账号已禁用，仅系统管理员可修改系统/连接外部服务。
- 需开放其他账号时，在数据库中设置 is_active=1 并重置密码。
